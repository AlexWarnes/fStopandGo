import React from 'react';
import './Resources.css';

export const Resources = (props) => {
  return (
    <div className="resources-view development">
      <h2>Feature is Under Development</h2>
      <p>The goal for the Resources page is to let users save links, videos, articles, etc that they find useful.</p>
    </div>
  )
}

export default Resources;